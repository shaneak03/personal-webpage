services:
  personal-webpage-dev:
    build:
      context: .
      dockerfile: Dockerfile
    image: personal-webpage:dev
    container_name: personal-webpage-dev
    restart: unless-stopped
    environment:
      NODE_ENV: production
      PORT: 3000
    ports:
      - "3001:3000"
